syntax = "proto3";
package gzv2ray.core.transport.internet.grpc.encoding;
option go_package = "github.com/gzjjjfree/gzv2ray-v4/transport/internet/grpc/encoding";

message Hunk {
  bytes data = 1;
}

service GunService {
  rpc Tun (stream Hunk) returns (stream Hunk);
}
